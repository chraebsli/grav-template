{% set logo = theme_var(mobile ? 'custom_logo_mobile' : 'custom_logo') %}
<a href="{{ home_url }}" class="navbar-brand mr-10 f row a c">
    {% if logo %}
        {% set logo_file = (logo|first).name %}
        <img src="{{ url('theme://images/logo/' ~ logo_file) }}" alt="{{ site.title }}" />
        <div class="web-title">
            <span class="desktop">Schützengesellschaft Rumisberg</span>
            <span class="mobile">SGR</span>
        </div>
    {% else %}
        {% include('@images/grav-logo.svg') %}
    {% endif %}
</a>